# Global
.env
.env.*
.DS_Store
Thumbs.db
.vscode/
.idea/

# Backend (Python/Django)
backend/.venv/
backend/**/__pycache__/
backend/**/*.py[cod]
backend/.mypy_cache/
backend/.ruff_cache/
backend/.pytest_cache/
backend/.tox/
backend/.coverage
backend/.coverage.*
backend/htmlcov/
backend/build/
backend/dist/
backend/**/*.egg-info/
backend/db.sqlite3

# Frontend (Node/Vite/TypeScript)
frontend/node_modules/
frontend/dist/
frontend/.vite/
frontend/.eslintcache
frontend/*.tsbuildinfo

# Docker
**/.docker-volume/

# Logs
logs/
**/*.log

# User uploads (Django MEDIA_ROOT)
media/
!media/.gitkeep

# Note: keep lock files committed for reproducible builds
!frontend/package-lock.json
!backend/uv.lock

# Ignore vscode AI rules
.github/instructions/codacy.instructions.md

**/**/db.sqlite3

# Android Firebase/Google Services config (must be provided via local file or CI secret)
android/app/google-services.json
# Network security config is required for Android builds.
# Keep it tracked; if local overrides are needed, they should be handled via flavors/manifest placeholders.
